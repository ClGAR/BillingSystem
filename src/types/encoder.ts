export type TargetRatio = {
  package: number;
  retail: number;
};

export type Leader = {
  id: string;
  name: string;
};

export type Expense = {
  id: string;
  category: string;
  amount: number;
};
